import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const Nav = () => (
  <nav>
    <ul>
      <li></li>
    </ul>
  </nav>
);

export default Nav;
